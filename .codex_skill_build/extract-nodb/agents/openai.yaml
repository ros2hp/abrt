interface:
  display_name: "Extract No DB"
  short_description: "Extract ABRT JSON from PL/SQL without DB load"
  default_prompt: "Use $extract-nodb to extract ABRT business-rule JSON from PL/SQL files in IN/ and write validated JSON to OUT/ without any database load."
